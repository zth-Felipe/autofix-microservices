server:
  port: 8092
spring:
  datasource:
    url: jdbc:postgresql://bd-vehiculos:5432/BD-Vehiculos
    username: postgres
    password: hola123
  jpa:
    hibernate:
      ddl-auto: update
  properties:
    hibernate:
      dialect: org.hibernate.dialect.PostgreSQLDialect
eureka:
  instance:
    hostname: localhost
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
